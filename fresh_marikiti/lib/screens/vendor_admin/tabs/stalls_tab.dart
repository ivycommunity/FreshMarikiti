import 'package:flutter/material.dart';

class StallsTab extends StatelessWidget {
  const StallsTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Stalls Tab', style: TextStyle(fontSize: 24)),
    );
  }
} 